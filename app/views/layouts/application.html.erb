<!DOCTYPE html>
<html>
  <head>
    <title>Ndl2018</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  	<body>
  		<div class="header">
  			<h1><%=link_to "SphimxScord", root_path%></h1>

  			<% if user_signed_in? %>

		        <%= link_to "Déconnexion", destroy_session_path(:user), method: :delete, class: 'right_top_button' %>

		    <% else %>

		        <%= link_to "Connexion", new_user_session_path, class: 'right_top_button'%>

		    <% end %>

  		</div>

	    <%= yield %>

	  </body>

	</html>
